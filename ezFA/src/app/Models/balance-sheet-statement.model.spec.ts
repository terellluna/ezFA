import { BalanceSheetStatement } from './balance-sheet-statement.model';

describe('BalanceSheetStatement', () => {
  it('should create an instance', () => {
    expect(new BalanceSheetStatement()).toBeTruthy();
  });
});
